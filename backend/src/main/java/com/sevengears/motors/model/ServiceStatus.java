package com.sevengears.motors.model;

public enum ServiceStatus {
    RECEIVED,
    INSPECTING,
    AWAITING_APPROVAL,
    IN_PROGRESS,
    QUALITY_CHECK,
    READY_FOR_PICKUP,
    DELIVERED,
    CANCELLED
}
